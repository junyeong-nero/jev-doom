[project]
name = "jev-doom"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "httpx>=0.28.1",
    "imageio>=2.37.4",
    "imageio-ffmpeg>=0.6.0",
    "pygame>=2.5.0",
    "vizdoom>=1.3.0",
]

[dependency-groups]
dev = ["pytest>=8"]
